{{#mdl-nav title="Razer Cars" scrollHeader=true}}
  {{#with-session as |session|}}
    {{!-- Example Nav Item --}}
    {{#if session.isAuthenticated}}
      {{mdl-nav-item name="Inventory" route="inventory"}}
      {{mdl-nav-item name="Car Types" route="car-type"}}
      {{mdl-nav-item name="Logout" route="logout"}}
    {{else}}
      {{mdl-nav-item name="Login" route="login"}}
    {{/if}}

    {{outlet}}

    {{#if session.isAuthenticated}}
      <table class="info-footer">
        <tr>
          <td>New Car Types: 20</td>
          <td>Cars Checked In: 100</td>
          <td>Cars Checked Out: 200</td>
        </tr>
      </table>
    {{/if}}
  {{/with-session}}
{{/mdl-nav}}
